{
  "name": "micro-frontend-container",
  "version": "1.0.0",
  "description": "Container que englobará todos os micro-frontends",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently --kill-others --kill-others-on-fail -p name --names \"server,container,restaurants,order\" \"npm run start-server\" \"npm run start-container\" \"npm run start-restaurants\" \"npm run start-order\"",
    "start-container": "cross-env NODE_ENV=development webpack-dev-server --watch --hot --config=config/webpack.config.js",
    "start-server": "cd ../server && npm start",
    "start-restaurants": "cd ../restaurants && npm start",
    "start-order": "cd ../order && npm start",
    "standalone-restaurants": "concurrently --kill-others --kill-others-on-fail -p name --names \"server,restaurants\" \"npm run start-server\" \"cd ../restaurants && npm run standalone\" ",
    "standalone-order": "concurrently --kill-others --kill-others-on-fail -p name --names \"server,order\" \"npm run start-server\" \"cd ../order && npm run standalone\" ",
    "build-container": "webpack --config=config/webpack.config.js -p",
    "install-all": "npm run install-container && npm run install-server, npm run install-restaurants && npm run install-order",
    "install-fronts": "npm run install-container && npm run install-restaurants && npm run install-order",
    "install-container": "npm install",
    "install-server": "cd ../server && npm start",
    "install-restaurants": "cd ../restaurants && npm start",
    "intall-order": "cd ../order && npm install",
    "remove-all": "rimraf node_modules && cd ../restaurants && rimraf node_modules && cd ../orders && rimraf node_modules"
  },
  "author": "Rene Souza",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/plugin-syntax-dynamic-import": "^7.8.3",
    "@babel/plugin-transform-runtime": "^7.10.1",
    "@babel/polyfill": "^7.10.1",
    "@babel/preset-env": "^7.10.2",
    "@babel/preset-react": "^7.10.1",
    "babel-loader": "^8.1.0",
    "concurrently": "^5.2.0",
    "core-js": "^3.6.5",
    "cross-env": "^7.0.2",
    "dotenv": "^8.2.0",
    "html-webpack-plugin": "^4.3.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11",
    "webpack-dev-server": "^3.11.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "react-redux": "^7.2.0",
    "react-router-dom": "^5.2.0",
    "redux": "^4.0.5",
    "redux-thunk": "^2.3.0"
  }
}
